// Minimal test setup to debug the issue
import '@types/jest';

console.log('✅ Minimal setup loaded successfully');

// Simple test helper without complex syntax
export function createSimpleTestData() {
  return {
    username: 'testuser',
    email: 'test@example.com'
  };
}
