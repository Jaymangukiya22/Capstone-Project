# QuizDash Audit Findings

Branch: `hotfix/error-handeling`, audited against the **working tree** (uncommitted edits included) as of 2026-07-18. Sections 1–2 originally documented findings only; **as of the second pass below, M1–M9 and H1/H2/H3 have been fixed in the working tree** (`backend/src/matchServerWorker.ts`, `backend/src/services/enhancedWorkerPool.ts`, `backend/src/config/database.ts`, `docker-compose.yml`, `nginx.conf`) and verified — see the status line on each finding and the **Fix verification** subsection at the end of Section 2. Sections 3–4 describe cleanup actions already executed (see commits `24345cf`..`c5c0d83` and [REMOVED.md](REMOVED.md)). **Section 5 is a third pass — a security-focused re-audit using bug-bounty hunting methodology (WebSocket, Node.js, race-condition, and TLS/network checklists) applied as source review against the whole repo, not just the match server. It found two critical, live-verified findings (S1, S3) that Sections 1–2 did not cover. Section 5 is document-only — nothing in it has been fixed yet.**

Labels used throughout: **[verified]** — read directly in the working-tree source and confirmed by a second check (grep for callers, Dockerfile trace, etc.); **[inferred]** — my best read of what a mechanism *will* do under a scenario I did not execute; **[remembered]** — restated from the prompt's ground truth, not independently re-derived here.

---

## SECTION 1 — Match server audit (master/worker path)

**[verified]** Production runs `matchServerMaster.ts` + `matchServerWorker.ts` + `services/enhancedWorkerPool.ts`. `backend/Dockerfile`'s `matchserver-master` stage (`CMD ["node", "dist/matchServerMaster.js"]`, line 83) is what `docker-compose.yml`'s `matchserver` service builds (`target: matchserver-master`, line 122). Neither `start.sh` nor `start-server.js` (which reference `matchServer-enhanced.js`) are invoked by any Dockerfile `CMD` — they're copied into images but dead as entrypoints. `matchServer-enhanced.ts` is therefore unreachable in production, confirming the ground truth; it was removed in Section 3. `matchWorkerPool.ts` is entirely commented out (373 lines, zero live code) — also dead, not further audited.

### [M1] `userToMatch` (master) never cleared on match completion
- **Status: FIXED** — `handleMatchCompleted`/`handleWorkerDeath`/`cleanupStaleMatches` now all route through one `teardownMatch()` that clears every `userToMatch` entry for the match via a new `matchToUsers` reverse index.
- File/line: `backend/src/services/enhancedWorkerPool.ts:166,211` (`.set()`), `handleMatchCompleted` at 187-208
- Category: memory-leak
- What happens: `handleMatchCreated` and `handlePlayerJoined` both `.set()` into the master-level `userToMatch: Map<number, string>`, but `handleMatchCompleted` — the normal, happy-path exit — never calls `.delete()` on it. The only delete site is `handlePlayerLeft` (line 215), which is unreachable: `matchServerWorker.ts` never sends a `{type:'player_left'}` message to the master, so that handler is dead code.
- Trigger: any match that runs to completion normally (the common case).
- Maps to which reported bug: none/latent — this is a slow leak, not an acute hang.
- Severity: medium, silent (no error surfaces; just grows).
- Blast radius at 6K matches: scales with **unique users over the process lifetime**, not concurrent matches — a long-lived master process under sustained 12K-connection traffic accumulates one stale entry per user per match played, unbounded until process restart.
- Fix sketch: delete the entry (or overwrite is already implicit for repeat players) inside `handleMatchCompleted`, keyed off each player in the completed match.
- How to verify: track `userToMatch.size` via a gauge over a soak test with N unique users playing M matches each; it should plateau near N, not grow toward N×M.

### [M2] `trackedMatches` Set leaks on every exit path except live completion
- **Status: FIXED** — same `teardownMatch()` centralization as M1 clears `trackedMatches` on all three exit paths (completion, worker death, idle sweep).
- File/line: `enhancedWorkerPool.ts:33` (declaration), `.add()` at 170, `.delete()` only at 196
- Category: memory-leak / unbounded-growth
- What happens: `trackedMatches` is only cleaned up inside `handleMatchCompleted` (the message a *live* worker sends). It is never touched by `handleWorkerDeath` (worker killed by the reaper) or `cleanupStaleMatches` (idle-match sweep) — both of those paths remove the match from `matchToWorker`/`matchLastActivity` but leave it in `trackedMatches` forever.
- Trigger: any match that ends via worker crash/reaper-kill or idle-timeout rather than a clean both-players-finish.
- Maps to which reported bug: latent — feeds into #M6/#M9's "matches hang after worker restart" story since it's a symptom of the same missing cleanup path, but by itself it's just a growing Set.
- Severity: medium, silent.
- Blast radius at 6K matches: scales with the *rate of abnormal match endings*, which is exactly what's elevated by the other bugs in this list — a compounding leak, not an independent one.
- Fix sketch: centralize match-teardown into one function called from all three exit paths (`handleMatchCompleted`, `handleWorkerDeath`, `cleanupStaleMatches`) instead of duplicating partial cleanup in each.
- How to verify: force a worker kill (`kill -9` a worker PID) mid-match and confirm `trackedMatches.size` decrements afterward.

### [M3] No server-side fallback for a stalled question — "questions don't advance" root cause
- **Status: FIXED and live-verified.** `questionTimers` is now actually used: `scheduleQuestionTimer()` sets a server-owned deadline (`timeLimit + QUESTION_TIMEOUT_GRACE_SECONDS`) in `startMatch()`/`nextQuestion()`, cleared in `endMatch()`/`terminateStaleMatch()`, and re-armed for matches hydrated mid-`IN_PROGRESS` from Redis (`ensureQuestionTimerForHydratedMatch()`, covering the worker-restart case). On timeout, `handleQuestionTimeout()` force-submits a blank/zero-point answer for whoever hasn't submitted and advances. Verified against the real running stack (Docker Compose, `docker compose build backend matchserver` + a live socket.io script): player A submitted, player B never did, and the match force-advanced via `question_timed_out` → `next_question` at ~35s instead of hanging.
- File/line: `backend/src/matchServerWorker.ts:59` (`questionTimers: Map<string, NodeJS.Timeout>`, declared, **never `.set()` or `.delete()` anywhere in the file**); gating logic at `submitAnswer()` lines 836-846; `nextQuestion()` 860-884
- Category: reconnection-gap (functionally: unhandled-hang)
- What happens: question advance is **purely both-players-submitted-driven**:
  ```
  836  const allSubmitted = Array.from(match.players.values()).every(
  837    p => p.hasSubmittedCurrent
  838  );
  839  if (!allSubmitted) { ...; return; }
  ```
  `match.timeLimit` is sent to the client (`sanitizeQuestion`) and enforced *client-side only*; the server never force-advances on a timeout. `questionTimers` is scaffolding for a per-question timeout that was never wired up.
- Trigger: player B disconnects (or their tab hangs, or a client-side timer bug fires late) between submitting nothing and Q_n; player A submits and then waits. `allSubmitted` can never become true for that question.
- Maps to which reported bug: **"questions don't advance."** Directly.
- Severity: critical, silent — no error is emitted to either client; player A simply sits on `waiting_for_opponent` (line 844) forever.
- Blast radius at 6K matches: scales linearly with disconnect rate × matches, i.e. roughly with total concurrent connections (12K sockets) — at that scale, even a small per-connection drop rate produces many permanently-stuck matches per minute.
- Fix sketch: give every question a server-owned deadline (populate `questionTimers` with a `setTimeout` at `nextQuestion()`/`createMatch()` time, cleared on `allSubmitted`), and on timeout auto-submit a null/timed-out answer for whoever hasn't submitted, then advance. The stale-match reaper (5-min inactivity, `reapStaleMatches`, worker.ts:90-100) is the *only* current escape hatch, and it kills the whole match rather than the one question — a much worse user experience than a per-question timeout.
- How to verify: in an integration test, join two players, have only one submit, and assert the match advances (or ends) within `timeLimit + margin` seconds instead of hanging until the 5-minute reaper fires.

### [M4] Per-worker match-capacity check (`MAX_MATCHES`) is unreachable dead code
- **Status: PARTIALLY FIXED.** `createMatch()`'s own `MAX_MATCHES` check is still unreachable dead code (the live path is still `joinMatch()`'s implicit creation, unchanged). What's fixed is the *effect* this dead code was supposed to have: `assignMatch()` (M5) now enforces a real ceiling at the pool level, so the practical capacity gap this finding described is closed even though the specific dead code wasn't touched. Left as-is deliberately rather than deleted, since `MAX_MATCHES` is still read/reported in `getStats()`/`worker_ready`.
- File/line: `matchServerWorker.ts:14,188` (`createMatch()` gate); `matchServerMaster.ts` join handlers (617-975) only ever forward `{type:'join_match'}`, never `{type:'create_match'}`
- Category: dead-code / resource-contradiction
- What happens: `createMatch()` throws once `this.matches.size >= MAX_MATCHES` (default 5), but nothing in the master ever sends the `create_match` message that would invoke it — matches are always created implicitly inside `joinMatch()` (worker.ts:280-347, "hydrate from Redis"), which has **no size check at all**.
- Trigger: sustained match creation on a single worker under the current live code path.
- Maps to which reported bug: contributes to "match hangs" indirectly — see M5.
- Severity: high, silent (no rejection, no error — a worker just keeps absorbing matches).
- Blast radius at 6K matches: this is a *capacity ceiling that doesn't exist* — the documented capacity math (500 workers × 5 matches = 2,500) assumes this cap is enforced; since it isn't, a single overloaded worker's actual concurrent-match count is bounded only by `assignMatch()`'s load-balancing behavior (see M5), not by a hard per-worker limit.
- Fix sketch: add the same size check to `joinMatch()`'s match-creation branch (or better, reject at `assignMatch()` time — see M5 — so the master never routes a new match to a full worker in the first place).
- How to verify: spin up one worker, force-route N synthetic matches to it, and confirm creation is rejected once `MAX_MATCHES` is hit — currently it will not be.

### [M5] `assignMatch()` never rejects an over-capacity worker
- **Status: FIXED.** `assignMatch()` now returns `null` (which every call site already handled — surfaces `NO_AVAILABLE_WORKERS` to the client) once every worker is at/over its declared capacity (`minLoad >= 1`) and `MAX_WORKERS` has been reached, instead of always assigning the least-bad option.
- File/line: `enhancedWorkerPool.ts` `assignMatch()`, load calc at line 261, "no workers" branch at 273-276
- Category: unbounded-growth
- What happens: the function always returns the least-loaded existing worker, with no `load >= 1` rejection branch — the only `null` return is "there are zero workers." Combined with M4 (no per-worker hard cap in the live join path), a worker pool that's fully saturated will keep stacking matches onto whichever worker is *least* saturated rather than refusing new matches or forcing a scale-up-and-wait.
- Trigger: match-creation rate exceeding `checkAndScale()`'s scale-up reaction time (5s check interval, but new workers take real wall-clock time to spawn and pass the 2-min health-check grace period).
- Maps to which reported bug: "match hangs" / general degradation under load — an overloaded worker process (event loop contention, GC pressure) makes *every* match it holds laggy, which surfaces to users as stuck-feeling matches even without a logic bug like M3.
- Severity: high, silent — degrades rather than errors.
- Blast radius at 6K matches: directly proportional to gap between demand and worker scale-up speed; worst during traffic spikes, which is exactly when 6K-match capacity would be stress-tested.
- Fix sketch: `assignMatch()` should refuse (return `null`) once every worker's load ≥ some ceiling, forcing the caller (master's join handler) to hold/queue the join briefly while `checkAndScale()` spins up capacity, rather than silently overloading one process.
- How to verify: load-test past the proactive-spawn threshold and graph per-worker match count — today it can exceed `MAX_MATCHES_PER_WORKER` on a single worker; after a fix it should plateau at the ceiling.

### [M6] Worker-death reaper races its own graceful shutdown and never touches Redis
- **Status: FIXED.** `handleWorkerDeath()` now clears the stale `workerId` cached in each affected match's Redis blob, so a rejoin re-triggers `assignMatch()` instead of routing back to the dead worker. The health-check reaper now attempts `send({type:'shutdown'})` before `.kill()` (best-effort — the worker may already be unresponsive). Scale-down now waits 2s after the shutdown message before force-killing, instead of killing in the same tick.
- File/line: `enhancedWorkerPool.ts` `startHealthChecks()` 460-481 (kills after 3 min no-heartbeat, 2-min startup grace), `handleWorkerDeath()` 218-241, scale-down `idleWorkers.forEach` 448-453 (`worker.send({type:'shutdown'}); worker.kill();` back-to-back, no wait)
- Category: race-condition
- What happens: both the reaper and the scale-down path call `.kill()` immediately (reaper doesn't even send a shutdown message first; scale-down sends one but kills in the same tick without waiting for the worker's own `shutdown()` — worker.ts:1208-1212 — to run). `handleWorkerDeath` cleans master's own `matchToWorker`/`matchLastActivity` maps and tells connected clients to rejoin (`match_error`, "Match server restarted"), but it **never touches the Redis `match:{matchId}` key**, which still carries the dead worker's ID until its TTL (3600s) expires or something overwrites it.
- Trigger: a worker goes quiet for 3 minutes (real crash, or just GC/event-loop stall from M5's overload), or a scale-down fires while the worker is mid-request.
- Maps to which reported bug: **"one joins, other stuck loading"** is the most plausible fit — if player A's `join_match` is forwarded to a worker that dies (or is scale-down-killed) between player A's join and player B's join, the master's bookkeeping is cleared and B's join gets routed to a fresh worker with no memory of A, while A was already told `match_joined`/`LOAD_GAME_SCENE` by the now-dead worker and has no signal that anything went wrong until (if ever) a stale-match timeout elsewhere fires.
- Severity: critical when it hits an in-progress join, silent to the affected client (the `match_error` broadcast only reaches sockets still in the Socket.io room on redis-adapter, which depends on the room actually having been joined before the process died — timing-dependent).
- Blast radius at 6K matches: proportional to worker crash/restart rate × concurrent-join rate; low probability per event but the population size (500 workers, thousands of joins/minute at target load) makes it a background drip of "stuck" reports rather than a one-off.
- Fix sketch: (a) have `handleWorkerDeath` delete or reassign the Redis `match:{matchId}` key's `workerId` field so a subsequent join re-triggers `assignMatch()` cleanly instead of finding a stale worker reference; (b) give scale-down a real grace period (await an ack or a fixed delay) before `.kill()`, and don't kill via the health-check reaper without at least attempting a `send({type:'shutdown'})` first — right now the health-check path is a hard kill with zero grace.
- How to verify: kill a worker holding an in-progress join (one player joined, second not yet) and confirm the second player's join lands cleanly on a new worker within the 30s reconnection window, and the first player's client receives an actionable error rather than an indefinite loading state.

### [M7] Reconnection resync omits opponent state
- **Status: FIXED.** `match_reconnected` now includes an `opponent` object (`userId`, `username`, `connected`, `score`, `hasSubmittedCurrent`) alongside the reconnecting player's own state.
- File/line: `matchServerWorker.ts:357-389` (`joinMatch()` reconnect branch)
- Category: reconnection-gap
- What happens: on reconnect during `IN_PROGRESS`, the server sends the reconnecting player's own question/score/answers/submission-status, but **no opponent connection status, opponent score, or opponent submission status**. The client has to infer whether the other player is even still connected.
- Trigger: any client refresh mid-match.
- Maps to which reported bug: contributes to "one joins other stuck loading" — a refreshed client that can't tell whether the opponent is present or gone has no way to distinguish "waiting for opponent to answer" from "opponent disconnected and match is effectively dead" (which is exactly what M3 makes possible).
- Severity: medium — not a hang by itself, but it removes the UI's ability to disambiguate a hang from normal waiting.
- Blast radius at 6K matches: every refresh mid-match hits this gap; scales with reconnection frequency, which the constraints call a first-class requirement ("refresh always recovers").
- Fix sketch: include opponent's `socketId != null` (connected), `hasSubmittedCurrent`, and `score` in the `match_reconnected` payload.
- How to verify: refresh one client mid-question and assert the resync payload includes a field the UI can use to render "opponent connected/disconnected" state, not just infer it from timeouts.

### [M8] `endMatch()` serializes DB writes ahead of the completion emit
- **Status: FIXED.** `match_completed` now emits immediately after the results are computed; the Postgres `Match`/`MatchPlayer` writes moved into a fire-and-forget `.then()` chain (same pattern `submitAnswer` already used) and the per-player loop now runs via `Promise.all` instead of sequential `await`s.
- File/line: `matchServerWorker.ts` `endMatch()` 920-965 — sequential `await this.ensureDbMatch(match)` → `await matchDb.update(...)` → per-player loop with `await MatchPlayerModel.findOne/create/update` (930-951, in a `for` loop, not `Promise.all`) — all before `match_completed` emit at 959-965
- Category: blocking-hot-path
- What happens: both players' "match complete" notification is delayed by however long the full sequential DB write chain takes, unlike `submitAnswer()` (line 793 `saveMatchState`, Redis-only, then the fire-and-forget `.then()` chain at 819-834 explicitly kept off the critical path per the code's own comment). `endMatch` doesn't get the same treatment.
- Trigger: every match completion.
- Maps to which reported bug: none/latent at low load; contributes to general perceived latency at scale.
- Severity: low-medium, loud in the sense that it's measurable latency, not silent failure.
- Blast radius at 6K matches: linear in matches-completing-per-second × DB round-trip time × (1 + player count) since the player loop is sequential, not parallel — compounds with the Sequelize pool being hardcoded to `max: 10` (Section 2, H1), which is the more severe version of this same class of problem.
- Fix sketch: emit `match_completed` immediately after `saveMatchState`/Redis update, then run the Sequelize persistence off the critical path the same way `submitAnswer` already does — the pattern to copy already exists in this file.
- How to verify: measure time from last `submitAnswer` to client-received `match_completed` before/after; should drop to roughly the Redis round-trip.

### [M9] Scale-down capacity check trusts `matchCount` bookkeeping that can desync from the worker's real state
- **Status: FIXED (code-level; not separately load/race-tested).** Master's `cleanupStaleMatches()` now sends `{type:'terminate_match', matchId}` to the owning worker, which calls a new public `forceTerminateMatch()` (routes to the existing `terminateStaleMatch()`), so the worker's own match state is torn down on the same signal instead of two independent 5-minute timers potentially disagreeing. The race window is narrowed, not provably eliminated — I did not instrument both reapers' fire times to confirm zero overlap under load, per the original "how to verify" note.
- File/line: `enhancedWorkerPool.ts` `checkAndScale()` 429-455 (guard: `w.matchCount === 0 && w.status === 'idle'`), `cleanupStaleMatches()` 353-388 (decrements `matchCount` for matches idle >5 min at master's clock, called from inside `checkAndScale()` at line 391) vs. worker's own independent 5-min reaper (`matchServerWorker.ts` `reapStaleMatches`, 90-100)
- Category: race-condition
- What happens: two independent 5-minute idle-match reapers exist — master's `cleanupStaleMatches` (decrements `matchCount` without notifying the worker) and the worker's own `reapStaleMatches` (actually deletes from `this.matches`). They run on different clocks/timers with no coordination. A narrow window exists where master believes a worker is idle (`matchCount === 0`) and scale-down-kills it while the worker process still has a match object mid-cleanup.
- Trigger: worker-pool scale-down coinciding with the ~5-minute idle boundary on a match.
- Maps to which reported bug: latent contributor to M6's family of symptoms — same "worker killed while still relevant" shape, narrower window.
- Severity: low (narrow race window), silent.
- Blast radius at 6K matches: low probability per worker but scales with worker churn, which scale-up/down cycles make more frequent at the target load than at today's scale.
- Fix sketch: have master's stale-match cleanup message the worker to confirm/trigger its own cleanup (single source of truth) instead of running two independent timers against the same data.
- How to verify: instrument both reapers' fire times in a soak test and look for overlap windows where master decrements `matchCount` to 0 while the worker's `this.matches` for that ID is still populated.

### Async-handler / unhandled-rejection audit — no findings
**[verified]** Every `async` `socket.on`/message handler in `matchServerMaster.ts` (`authenticate`, `start_auto_matchmaking`, `create_friend_match`, `join_match`, `join_match_by_code`, `connect_to_match`, `client_closing`, the forwarded-events loop) wraps its body in try/catch. `matchServerWorker.ts` has no direct `socket.on` handlers (it's driven by `process.on('message', ...)`, which is also try/catch-wrapped). This category, called out as a hunt target in the brief, came back clean — noted for completeness rather than padded into a finding.

### Ranked shortlist — most likely causes of the reported bugs

1. **[M3] No server-side question-advance fallback** — names "questions don't advance" exactly, is unconditional (fires on the very first mid-question disconnect, not just under load), and is the most concretely verified (the `questionTimers` Map is provably dead code, zero call sites). **Top pick.**
2. **[M6] Worker-death/scale-down races leaving a client mid-join with no signal** — best fit for "one joins, other stuck loading," but is probabilistic (needs a worker death/kill to land in the join window) rather than deterministic like M3.
3. **[M5]/[M4] Missing capacity ceiling in the live join path** — doesn't directly cause a named bug but is the load-bearing reason M6 becomes *likely* rather than rare: without a real per-worker cap, workers get overloaded, which raises crash/stall probability, which feeds M6.
4. **[M9] Dual uncoordinated stale-match reapers** — same failure family as M6, lower probability, worth fixing in the same pass.
5. **[M1]/[M2] Map/Set leaks** — not acute-hang causes, but at 12K sustained connections they're the kind of slow leak that eventually forces process restarts, which then *trigger* M6's failure mode. Worth fixing alongside, not first.

**Strongest case against M3 being the single root cause:** the resource-contradiction in [H-section below] is real and severe (docker-compose limits are ~2.3x the host's RAM and ~3.2x its cores) — under actual memory pressure, the OOM killer or CPU starvation could produce symptoms that *look* like "questions don't advance" (an unresponsive process rather than a logic gap) without M3 being involved at all. The distinguishing test: if disconnect-triggered hangs occur even on a lightly-loaded single match with no memory/CPU pressure, that isolates M3 as the cause; if hangs only appear under concurrent load, the resource ceilings are the more likely primary cause and M3 is a secondary, compounding bug. I have not run that test — this ordering is inferred from code reading, not from a load test.

---

## SECTION 2 — HTTP/Express + Socket.io app-server audit

**[verified]** Several items the brief flagged as likely-present issues are **already fixed** in the current uncommitted working tree (visible in `git diff --stat`: `authController.ts`, `middleware/auth.ts`, `routes/authRoutes.ts`, `utils/auth.ts` are all modified, plus a new untracked `middleware/rateLimiter.ts`). Calling this out explicitly since it changes the shape of this section from "here's what's broken" to "here's what's already fixed vs. what remains":

- **Auth hot path caching — already fixed.** `middleware/auth.ts:24-25,90,119-125` caches the authenticated user in Redis for 60s (`user:{id}:auth`), falling back to `User.findByPk` (line 98-100) only on cache miss. Not a finding.
- **Register TOCTOU — already fixed.** `authController.ts:16-26` creates directly and catches `UniqueConstraintError` (lines 64-71) instead of a `findOne`-then-`create` race; `models/User.ts` has `@Unique` on both `username` (32-35) and `email` (37-40). Not a finding.
- **Login enumeration — already mitigated.** Both the not-found branch (110-120, with a dummy bcrypt compare against `DUMMY_PASSWORD_HASH` for timing equalization) and the wrong-password branch (134-140) return an identical `INVALID_CREDENTIALS` error/message. Not a finding.
- **`lastLoginAt` write — already fire-and-forget.** `authController.ts:143-147`, explicitly not awaited, doesn't block the login response. Not a finding.
- **bcrypt — native, not bcryptjs.** `package.json` declares `bcrypt: ^6.0.0`; no `bcryptjs` dependency exists; actual imports (`utils/auth.ts`, `quickSeed.ts`, `engineeringSeeder.ts`) all use native `bcrypt`. Not a finding.
- **Rate limiting — present on the endpoints that matter, one gap.** `routes/authRoutes.ts:11-13` applies `authLimiter` (10 req/15min/IP, from the new untracked `middleware/rateLimiter.ts`) to both `/register` and `/login`. `/refresh` has **no rate limiter** — worth closing since a refresh-token endpoint is also bcrypt/JWT-verification work that could be hammered, though lower severity than register/login since it requires a valid refresh token to do anything.

### [H1] Sequelize pool hardcoded to `max: 10`, ignoring the env vars meant to configure it
- **Status: FIXED.** `pool.max`/`min`/`acquire`/`idle` now read `DB_POOL_MAX`/`DB_POOL_MIN`/`DB_POOL_ACQUIRE_TIMEOUT`/`DB_POOL_IDLE_TIMEOUT` (same defaults as before if unset, so no behavior change unless the vars are set). `docker-compose.yml` already set `DB_POOL_MIN=20`/`DB_POOL_MAX=2000` for `backend` — that was previously silently ignored and now takes effect. Added a separate, smaller `DB_POOL_MIN`/`MAX` (`MATCHSERVER_DB_POOL_MIN:-5` / `MATCHSERVER_DB_POOL_MAX:-50`) for `matchserver`, since each of its worker *processes* gets its own pool and the same 2000 figure per-process would be reckless multiplied across workers.
- File/line: `backend/src/config/database.ts:38-43`
  ```
  pool: { max: 10, min: 0, acquire: 30000, idle: 10000 }
  ```
- Category: resource-contradiction / blocking-hot-path
- What happens: this is a literal, hardcoded object — `DB_POOL_MAX`/`DB_POOL_MIN` (defined in `.env`, `.env.self-hosted`, and read into `docker-compose.yml`'s env block) are never referenced anywhere in `backend/src` **[verified via repo-wide grep]**. Meanwhile Postgres itself is tuned for `max_connections=5000` (`docker-compose.yml:20` and command args). All that headroom is unusable because the application never opens more than 10 pooled connections per process.
- Trigger: any request volume exceeding ~10 concurrent DB-bound requests per backend/matchserver process.
- Severity: high, loud in one sense (requests will start timing out at the 30s `acquire` ceiling and throwing) but easy to misdiagnose as "the database is slow" rather than "the pool is starved," since Postgres itself will show low connection counts.
- Blast radius at 6K matches: this is likely the single biggest gap between "what the infra was tuned for" (5000 Postgres connections) and "what the app can actually use" (10) — at 6K concurrent matches with any DB-backed operation per question/match-end (see M8), a pool of 10 is trivially saturated.
- Fix sketch: wire `pool.max`/`pool.min` to `process.env.DB_POOL_MAX`/`DB_POOL_MIN` with sane fallbacks, and size them per-process against the shared `max_connections=5000` budget (backend + matchserver process counts × pool size should stay well under that ceiling).
- How to verify: under load, graph Sequelize pool "waiting for connection" time (or acquire-timeout error rate) before/after — should drop to ~0 once the pool matches actual concurrency.

### [H2] Hardcoded round-number ceilings — most likely candidate for "sticks at exactly 10,000"
- **Status: RE-VERIFIED, partially retracted, and fixed.** `nginx.conf:1` has `worker_processes auto;`, which resolves to 4 on this host — so the effective ceiling was actually `10000 × 4 = 40,000`, not `10,000` as I'd flagged with an `[inferred]` caveat. **This specific number is not the "sticks at 10K" culprit; retracting that part of the original finding.** However, verifying it surfaced a real, separate bug: neither `nginx.conf` nor the nginx service in `docker-compose.yml` set `worker_rlimit_nofile`/`ulimits`, so `worker_connections 10000` was silently capped by the container's inherited OS file-descriptor ulimit (commonly 1024) — the *configured* number was fiction regardless of the multiplication. **Fixed**: added `worker_rlimit_nofile 65535;` to `nginx.conf` and a matching `ulimits.nofile` block (65535/65535, mirroring what `matchserver` already had) plus a higher default `NGINX_CPU_LIMIT` (0.25 → 1.0 core) to `docker-compose.yml`'s `nginx` service, since 0.25 cores was thin for a proxy meant to handle a 12K-connection target.
- File/line: `nginx.conf:4` (`worker_connections 10000;`), `redis.conf:41` (`maxclients 10000;`), `.env` / `.env.self-hosted` (`DB_POOL_IDLE_TIMEOUT=10000` — this one is milliseconds, unrelated to a connection count, ruled out)
- Category: resource-contradiction
- What happens: `nginx.conf`'s `worker_connections 10000` is a **per-nginx-worker-process** ceiling; the effective total capacity is `worker_connections × worker_processes`, and each proxied client typically consumes 2 file descriptors (client-facing + upstream), roughly halving effective client capacity per worker. I did not confirm nginx's `worker_processes` setting in this pass **[inferred, not verified]** — if it's `1` (or `auto` resolving to a low core count under the 4-core budget), the real ceiling could functionally already be close to 10,000 total connections or even ~5,000 concurrent clients, which would explain a hard stop at a suspiciously round number.
- Trigger: aggregate concurrent connections approaching nginx's configured ceiling.
- Severity: critical if this is the actual wall, since it silently drops/refuses connections rather than degrading.
- Blast radius at 6K matches (12K sockets target): if `worker_connections 10000` is the effective ceiling, the app cannot reach the 12,000-socket target *by construction*, independent of any application-level fix.
- Fix sketch: raise `worker_connections` well above the 12K target (with matching `worker_rlimit_nofile`), and confirm `worker_processes` isn't multiplying/dividing that ceiling unexpectedly.
- How to verify: load-test past 10,000 concurrent connections and watch nginx's `stub_status`/error log for `worker_connections are not enough` — this message, if present, is a smoking gun.
- **Strongest case against this being the wall**: H1 (Sequelize pool `max: 10`) would cause request-level failures (500s, timeouts) at a much lower concurrency than 10,000 raw socket connections, especially since Socket.io connections themselves don't need a DB connection just to stay open — only request-driven operations do. If the "stuck at 10K" symptom is about *successful matches/requests* rather than raw socket count, H1 is the more likely culprit and H2 is coincidental. I did not have access to what "stuck at 10K" was measured against (connections vs. requests vs. matches) to disambiguate — **flagging both, ranked by which literal number matches the report.**

### [H3] Console logging left on by default even in production
- **Status: FIXED.** `docker-compose.yml`'s default for both services flipped to `LOG_TO_CONSOLE:-false`; still overridable per-environment via the env var.
- File/line: `backend/src/utils/logger.ts:49-75`; `docker-compose.yml:94,156` (`LOG_TO_CONSOLE: ${LOG_TO_CONSOLE:-true}` for both `backend` and `matchserver`)
- Category: blocking-hot-path
- What happens: `shouldLogToConsole` is true whenever `NODE_ENV !== 'production'` **or** `LOG_TO_CONSOLE === 'true'` — and the compose file's own default sets `LOG_TO_CONSOLE=true` regardless of `NODE_ENV`. The code itself already warns about this (lines 55-63, a `console.warn` emitted specifically when this combination is detected) — the awareness exists, the default doesn't reflect it. Winston's Console transport writes synchronously to `process.stdout` with no buffering, per the code's own comment (56-59).
- Severity: medium — compounds under sustained high-throughput logging (e.g., per-request or per-socket-event logs), not a hang by itself.
- Blast radius at 6K matches: scales with log-line volume, which scales with match/question/connection event volume — exactly what's elevated at target load.
- Fix sketch: flip the compose default to `LOG_TO_CONSOLE:-false` for anything running with `NODE_ENV=production`, relying on the file transport (which is already async-appropriate, 5MB/5-file rotation).
- How to verify: compare event-loop lag / request p99 with `LOG_TO_CONSOLE=true` vs `false` under the same load profile.

### Ranked shortlist and the 10K question

1. **[H1] Sequelize pool hardcoded to 10** — most severe, most certain to bite before 6K matches is reached, and independently confirmed by grep (zero references to the env vars meant to control it).
2. **[H2] nginx `worker_connections 10000`** — best literal match for "sticks at exactly 10,000," but unverified against `worker_processes`; my answer to "what's causing the hard stop at 10K": **this is the strongest literal-number match in the codebase**, with the caveat above that H1 could produce a similar-looking wall at a different, lower number that happens to get misreported as "around 10K" if nobody's measuring precisely.
3. **[H3] Synchronous console logging under production defaults** — real but secondary; a latency multiplier, not a hard ceiling.

### Fix verification (second pass)

All of M1–M9 and H1–H3 above were implemented in the working tree and checked as follows:
- **[verified]** `npx tsc --noEmit` — clean, zero errors.
- **[verified]** `npm run build` — clean, including the `prebuild` step (confirms the Section 3 `pre-build.js` fix still holds end-to-end).
- **[verified]** `npm test` against a real Postgres + Redis (via `docker compose up postgres redis`, pointed at by env vars) — 157/171 pass. The 14 failures are all pre-existing and unrelated to this pass's changes: `authController`/`quizController`/`categoryController` integration tests asserting old free-text error-message substrings (e.g. `toContain('already exists')`) against the already-changed-before-this-session machine-readable error codes (`USER_ALREADY_EXISTS`, `VALIDATION_ERROR`, `AUTH_REQUIRED`, etc.) — a test/implementation drift issue, not something introduced by the match-server fixes, and out of this pass's scope.
- **[verified]** `docker compose build backend matchserver` — both images build clean on the current Dockerfile stages.
- **[verified]** Full live run: brought up `postgres`, `redis`, `backend`, `matchserver` via `docker compose up`, confirmed both `/health` endpoints report healthy, then ran a live `socket.io-client` script against the running `matchserver` that created a real friend match, joined two players, had only one submit an answer, and confirmed the match force-advanced via `question_timed_out` → `next_question` instead of hanging — direct proof the M3 fix (the top-ranked root cause) works against the actual running system, not just in isolated reasoning. Containers were stopped (not removed) afterward.
- **[not verified]** M9's race-window narrowing (no dedicated concurrent load test); the exact magnitude of M8's latency improvement (no before/after p99 measurement); H2's fix under an actual 12K-connection load test.

---

## SECTION 3 — Dead code & config removal (executed)

See [REMOVED.md](REMOVED.md) for the itemized list with commit references. Summary of what was **not** executed and needs your call:

- **`.env.network`** (private-network mode) — left in place; it's gitignored/untracked, so deleting it isn't git-reversible. `deploy.sh`/`deploy.bat`/`deploy-all.*` (the latter already removed) still offer a "network" mode that depends on it — recommend stripping that mode from `deploy.sh`/`deploy.bat`'s menu in a follow-up pass, since editing their control flow safely needs a closer read than this pass budgeted for.
- **12 overlapping deployment-guide markdown files** (`DEPLOYMENT_CHECKLIST.md`, `DEPLOYMENT_GUIDE.md`, `DEPLOYMENT_MODES.md`, `DEPLOYMENT_QUICK_START.md`, `DEPLOYMENT_SUMMARY.md`, `PRODUCTION_DEPLOYMENT_GUIDE.md`, `PRODUCTION-2000-MATCHES-GUIDE.md`, `QUICK_START.md`, `QUICK_REFERENCE.md`, `README_DEPLOYMENT.md`, `SCALING_CONFIGURATION.md`, `SCALING_SUMMARY.md`, `SETUP_GUIDE.md`, `DOCUMENTATION_GUIDE.md`, `INFRASTRUCTURE_UPDATES.md`) — not deleted; genuinely need a human call on which content survives a `docs/` consolidation versus which is stale. Recommend picking one canonical deployment doc + one canonical scaling doc and archiving the rest.
- **`docker-stack.yml`** (Swarm/multi-node) — left in place, not deleted. It doesn't match the stated single-16GB/4-core-host target and has no cloudflared wiring either, but it represents real prior work and might matter for the stated "possible future upgrade" path — flagging for your call rather than deleting.
- **Orphaned `.env` variables** (29 of 79 root `.env` vars have zero code references — see the fact-finding pass for the full list; most infra/deploy-mode-related ones like `DEPLOYMENT_MODE`, `CLOUDFLARE_ACCOUNT_ID/API_TOKEN/ZONE_ID`, `PRODUCTION_DOMAIN`, `COMPOSE_PROJECT_NAME` may still be consumed by the deploy shell scripts, which weren't in the grep scope) — listed, not pruned; editing `.env` risks breaking a deploy script that reads it outside the code paths I checked.
- **`Frontend-admin/src/services/matchClient.ts`** shows as modified in `git status` but wasn't in scope for this audit (admin frontend, not backend/matchserver) — untouched.

---

## SECTION 4 — Monitoring stack (documented, light cleanup only)

No monitoring service in `docker-compose.yml` declares `mem_limit`/`cpus` at all **[verified]** — `prometheus`, `grafana`, `node-exporter`, `postgres-exporter`, `redis-exporter`, `alertmanager`, `influxdb` are entirely unconstrained. On a 16GB/4-core host already overcommitted 2.3x on memory and 3.2x on CPU by the *application* services alone (Section 1's resource-contradiction, restated here: postgres 8G+redis 4G+backend 8G+matchserver 16G+nginx/frontend/adminer 1.5G = 37.5G declared vs 16G physical; cpus 2+1+3+6+0.75 = 12.75 vs 4 physical), seven more unconstrained monitoring processes compete for whatever's left — the exact resource contention this stack is meant to diagnose.

**Prometheus config**: only `monitoring/prometheus/prometheus.yml` is mounted (`docker-compose.yml:271`); it's the most complete of the three (5 scrape targets + alerting + `rule_files: alerts/*.yml`). The root `prometheus.yml` (2 targets) and `monitoring/prometheus.yml` (6 targets) are both **[verified]** unmounted and unreferenced by any compose service — dead config, but not byte-identical to each other or to the active one, so per the "exact dupes and empties only" rule for this pass, **not deleted** — flagged for the rebuild phase.

**Grafana dashboards** (21 files, none empty/sub-1KB): `radis.json` and `radis-exporter.json` are near-identical (same imported community Redis dashboard, different Grafana export versions) but **not byte-identical** (2,873 diff lines — different plugin/schema versions), so also not deleted this pass per the same rule. Broader groupings for the rebuild phase:
- *Working + useful*: `working-metrics-dashboard.json`, `match-capacity.json`, `api-latency-errors.json`, `bottleneck-detection.json`, `match-server-performance.json`, `MASTER-SYSTEM-DASHBOARD.json`
- *Likely duplicate pairs (candidates for a follow-up dedup, not deleted here)*: `radis.json` / `radis-exporter.json`; `postgresql-exporter.json` (92KB, full community dashboard) / `postgresql-prometheus.json` (4.5KB, hand-built); `quiz-match-metrics-dashboard.json` / `complete-quiz-match-metrics.json` / `quiz-match-simple-dashboard.json` (three overlapping match-metric dashboards of different completeness)
- *Not Prometheus-sourced, keep separately*: `cloudflare-DNS-analytics.json`, `cloudflare-analytics.json` (Cloudflare's own analytics API, not scraped metrics)
- *Smoke-test only*: `test-dashboard.json` (2 panels, `up`/`prometheus_build_info`)

**What's actually broken [verified via code cross-reference]:**
- `monitoring/alert_rules.yml`'s alerts query `quizup_http_requests_total`, `quizup_http_request_duration_seconds_bucket`, `quizup_active_users` — these metrics *are* registered in code (`backend/src/utils/metrics.ts`), but only served at `/metrics-custom` (`server.ts:213`), while every scrape config hits `/metrics` (served by `express-prom-bundle`, generic non-`quizup_`-prefixed names). **Prometheus never scrapes these metrics, so this entire alert group is permanently inactive.** This file also isn't inside the `alerts/` subdirectory the active prometheus.yml's `rule_files: alerts/*.yml` glob would pick up — doubly dead.
- `monitoring/prometheus/alerts/match-server-alerts.yml:142` (the one alert file that *is* actually loaded) references `pg_stat_activity_count`, which doesn't match either name postgres-exporter's custom query file actually produces (`pg_stat_activity_total_connections` / `pg_stat_activity_active_connections`, per `monitoring/postgres-exporter/queries.yaml`) — this specific alert will never fire.
- `monitoring/alert_rules.yml`'s `HighMemoryUsage` alert queries `container_memory_usage_bytes`/`container_spec_memory_limit_bytes` (cadvisor-style metrics) but no cadvisor service exists in any compose file — never scraped, never fires. (Same file is already dead per the point above, but worth noting the specific alert would be broken even if the file were wired up.)

**Gap list for the rebuild** (metrics that matter for this project's actual goals — crash-tolerant 6K-match capacity, leak-watching — that are currently missing):
- Per-worker live match count *as actually held by the worker* (`this.matches.size` in `matchServerWorker.ts`), not just master's `matchCount` bookkeeping — Section 1's M9 finding is exactly a desync between these two, and there's no metric today that would let anyone notice that desync happening.
- `questionTimers`/in-memory Map sizes (`userToMatch`, `trackedMatches`, `matchToWorker`, `matchLastActivity`) exported as gauges — M1/M2's leaks are invisible today; a `Gauge` per Map, scraped over time, would make them visible immediately instead of requiring a code read to find.
- Event-loop lag per worker process (Node's own `perf_hooks` event-loop-utilization or a lag-sampling gauge) — the best generic signal for "this worker is overloaded," relevant to M5's missing capacity gate.
- Orphaned/active match ratio — a match is "active" (worker holds it, clients connected) vs. "orphaned" (Redis key exists, no worker claims it, per M6) — currently indistinguishable from outside the process.
- Reconnection attempt/success rate — the constraints call refresh-recovery a first-class requirement; there's no metric today that would show whether it's actually working in production versus just in a manual smoke test.
- Sequelize pool saturation (`pool.max` vs. in-use vs. waiting-for-acquire) — directly relevant to H1.

---

## SECTION 5 — Security re-audit (bug-bounty methodology)

Originally a third pass (document-only), run at the user's request using the `bug-bounty`, `hunt-websocket`, `hunt-nodejs`, `hunt-race-condition`, and `hunt-tls-network` checklists — applied as **source-code review of the whole repo**, not a live-target engagement (no external URL, no scope page; this is the app's own team auditing their own code). Same finding format as Sections 1–2. **Update: S1 and S3 have since been fixed and live-verified (fourth pass), and two further findings S4/S5 were discovered and fixed in a fifth pass** — see the per-finding status lines. S2 remains document-only.

Per the bug-bounty skill's "only question that matters" — every finding below was checked against "can an attacker do this right now against a real user who took no unusual action, with concrete harm" before being written up. Four passed that bar decisively (S1, S3, S4, S5 — all now fixed); S2 is lower-severity (requires a matchId leak) and remains documented only. Several checklist categories (prototype pollution, SSTI, `child_process`/`eval` injection, SQL injection via raw queries, XSS via unsanitized template rendering) were checked and came back clean — see the "Checked, no finding" subsection so this doesn't read as an incomplete sweep.

### [S1] Match server WebSocket layer has no real authentication — client-supplied `userId` is trusted unconditionally
- **Status: FIXED and live-verified (fourth pass).** `matchServerMaster.ts`'s `authenticate` handler now requires a `token` field, verifies it with `jwt.verify()` against the same `JWT_SECRET` the REST API uses, and derives `userId`/`username` from the verified claims — never from the client payload. Also does the same Redis-cache-or-DB `isActive` check as `middleware/auth.ts` (reusing the exact same `user:{id}:auth` cache key), so a deactivated account can't keep using a still-valid JWT on the match server. Two client/server contract bugs that would have made this fix behave badly for real users were also fixed while in this code path: the server emitted `auth_error` but the client (`Frontend-admin/src/services/matchClient.ts`) listened for `authentication_error` (wrong event name — errors never reached the UI); and the server's `authenticated` payload never included the `success` field the client's own promise logic checked (`if (data.success)`), so every successful auth would have rejected the client-side promise. `authenticateUser()` in `matchClient.ts` now reads the real JWT from `localStorage.getItem('authToken')` (the same key `authService.ts` already writes) and includes it in the `authenticate` payload.
  
  **Live-verified** (real registrations/logins against the running stack, not synthetic tokens): (1) `authenticate` with no `token` → rejected with `auth_error`/`INVALID_TOKEN`. (2) `authenticate` with a garbage/forged token string → rejected the same way (signature check works). (3) `authenticate` with a real, valid JWT but a *mismatched* claimed `username` in the payload (`'irrelevant_claimed_name'`) → server responded with the **real** username decoded from the token (`s1vvictim...`), not the client's claim — proving identity is now derived from verified claims, not trusted input. A fourth test (own valid token + a spoofed `userId` in the payload) was designed but not completed live — the account's own `authLimiter` (Section 2, H-fix) correctly rate-limited the test script's repeated register/login calls before it could run; the identity-derivation proof from test 3 already covers the same mechanism (server ignores client-claimed identity fields regardless of which field is spoofed).
- **Severity: CRITICAL (as found). Live-verified with a working PoC before the fix, and live-verified fixed after.**
- File/line: `backend/src/matchServerMaster.ts:458-478` (the `authenticate` handler)
- Category: auth-bypass / broken-access-control (CSWSH-adjacent, but worse — no cookie/Origin trickery needed at all)
- What happens:
  ```
  458  socket.on('authenticate', async (data) => {
  459    try {
  460      const userId = data.userId || data.id;
  461      const username = data.username || `Player${userId}`;
  462      socket.data.userId = userId;
  463      socket.data.username = username;
  464      socket.emit('authenticated', { user: { id: userId, username } });
  ```
  There is no `io.use(...)` handshake middleware, no JWT/token check anywhere in `matchServerMaster.ts` or `matchServerWorker.ts` (confirmed via repo-wide grep — zero hits for `jwt`/`verifyToken` in either file), and no cross-check against the REST API's session at all. Any socket that connects to the matchserver (port 3001, directly reachable — no auth required just to open the connection) can claim to be **any** `userId` by sending it in the `authenticate` payload, and the server accepts it at face value.
  
  This isn't an oversight with no intended fix, either: the client-side type contract (`Frontend-admin/src/services/matchClient.ts:8-26`) already defines a `token?: string` field on the `authenticate` payload and an `authentication_error` response shape with `INVALID_USER` / `INVALID_TOKEN` / `USER_BANNED` codes — i.e., the protocol was *designed* for real verification. But `authenticateUser()` (`matchClient.ts:748-771`), the actual function the frontend calls, never populates that `token` field, and the server never checks for it even when absent. This is the exact "developer inconsistency = strongest evidence" pattern bug-bounty hunters look for: a security control that exists in the type system and is enforced nowhere at runtime.
  
  Since `socket.data.userId` is the identity used for every subsequent action — `join_match`, `join_match_by_code`, `connect_to_match`, `submit_answer`, `player_ready`, `CLIENT_READY`, `start_auto_matchmaking`, `create_friend_match`, and the forwarded-events loop's `matchId = data.matchId || await workerPool.getUserMatch(socket.data.userId)` lookup — this one gap compromises the entire match server's access control, not just one endpoint.
- Trigger: attacker needs only a victim's numeric user ID (sequential/enumerable per the seeded-data IDs observed during testing, e.g. `10001`, `174`, `6778`) and a WebSocket connection to port 3001. No password, no JWT, no cookie.
- **Live PoC (executed against the running stack, not theoretical):** created a real friend match between two real users (`victim`, `opponent`) via the actual `/api/friend-matches` REST endpoint and the actual match server, let it start normally. Then opened a **third, completely unrelated socket**, sent `authenticate` claiming `{userId: <victim's real id>, username: 'attacker_pretending_to_be_victim'}` — the server replied `authenticated` with the victim's real ID, no rejection. The attacker socket then emitted `submit_answer` with a garbage answer (`selectedOptions: [999999]`) and **no `matchId`** (the server resolved the victim's live match on its own via the impersonated ID). The victim's real, still-connected socket received `opponent_submitted` and `score_update` events reflecting the forged submission — the victim's live match was mutated by a socket the victim never opened, using an identity the attacker never proved.
- Maps to: not one of the originally-reported reliability bugs, but directly explains a *plausible* unreported failure mode ("my match score changed when I didn't submit anything" / "someone joined my match I didn't invite") that would look like a client bug rather than a security incident to anyone not specifically looking for it.
- Blast radius at 6K matches / 12K connections: every one of those connections is an unauthenticated identity claim. This isn't a rare-precondition bug — it's the default trust model for the entire match server.
- Fix sketch: the backend already issues real JWTs for the REST API (`backend/src/utils/auth.ts`, verified in Section 2). Reuse that: (a) client sends the real access token in `authenticate`'s `token` field (already typed, just never populated — `matchClient.ts`'s `authenticateUser()` needs to read it from wherever the REST client stores it and include it), (b) `matchServerMaster.ts`'s `authenticate` handler verifies that token with the same secret/logic as `utils/auth.ts` and derives `userId` from the verified claims, not from `data.userId`, rejecting with `auth_error`/`INVALID_TOKEN` on failure — which the client-side type contract already expects. This is a two-sided fix (client + server); did not implement it in this pass since it's a cross-cutting auth-architecture change, not a config tweak — flagging for an explicit decision before touching it.
- How to verify a fix: re-run the PoC above (available on request, deleted after this pass per the harness's temp-file convention) — it should fail at the `authenticate` step with `auth_error`/`INVALID_TOKEN` instead of succeeding.

### [S2] `connect_to_match` accepts a raw `matchId` from the client with no participant check
- **Severity: MEDIUM standalone; escalates to HIGH chained with S1.**
- File/line: `matchServerMaster.ts:856-955`
- Category: broken-access-control (IDOR-shaped — object reference is a UUID, so exploitability depends on the ID leaking)
- What happens: unlike `join_match`/`join_match_by_code` (gated by a 6-character `joinCode` looked up via `joincode:{code}` in Redis), `connect_to_match` takes `matchId` directly from the client and forwards straight to the worker with zero check that `socket.data.userId` is a participant or was ever invited. The worker's own gate (`joinMatch()`, `matchServerWorker.ts:486-491`) only blocks joining if the match is no longer `WAITING` or is already at `maxPlayers` — it doesn't check *who* is joining. So any authenticated-as-anything socket that learns a `matchId` for a match still in the lobby (one player joined, waiting for the second) can seat itself as that second player, no join code required.
- Trigger: attacker needs a `matchId` (UUID v4, not brute-forceable blindly) for a match still in `WAITING` status. Plausible leak vectors not fully explored in this pass: server logs (`logInfo` calls throughout `matchServerMaster.ts` include `matchId` at info level — see H3's note that `LOG_TO_CONSOLE` defaulted to `true` until this session's fix), the friend-match creation REST response (`matchId` is returned to the creator's own client, so it's only sensitive if it leaks *from* the creator, e.g. via a referrer header, shared screenshot, or browser history sync), or a future feature that lists/links matches by ID.
- Maps to: a variant of "one joins, other stuck loading"/wrong-opponent-joined complaints, if this were ever hit in practice — a third party seating themselves in a lobby meant for a specific invited friend.
- Blast radius: low standalone (requires matchId leakage), but combined with S1 it means an attacker doesn't even need a *real* second user's cooperation to fill a friend-match lobby — they can forge any identity and self-seat.
- Fix sketch: `connect_to_match` should verify the connecting `socket.data.userId` is either the match's creator or the join-code path was already used to register them as the second player, before forwarding to the worker — i.e., check against the Redis-stored `match.players` list, not just match status/capacity.
- How to verify: attempt `connect_to_match` with an arbitrary `matchId` and a `userId` that was never part of that match's creation/join flow; should be rejected once fixed.

### [S3] Redis is exposed on all interfaces with no authentication
- **Status: FIXED and live-verified.** Confirmed first that `redis.conf` (the file S3 originally cited) is **entirely unused** — `docker-compose.yml`'s `redis` service builds its own command line (`redis-server --maxmemory ... --maxmemory-policy ...`) and never mounts or references `redis.conf` at all, so that file's own `requirepass` line was always dead config regardless of whether it was commented out. The real fix went into the actual live config path: added `--requirepass "${REDIS_PASSWORD}"` to the `command:` line in `docker-compose.yml`, updated the `healthcheck` to authenticate, changed every `REDIS_URL`/`REDIS_ADDR`/`REDIS_HOSTS` consumer in that file (`backend`, `matchserver`, `redis-exporter`, `redis-commander`) to include the password, generated a real `REDIS_PASSWORD` into `.env`, and changed the `ports:` mapping from `0.0.0.0`/`[::]` to `127.0.0.1` only (backend/matchserver reach Redis over the internal `quizup_network` by service name — the host-published port was never needed for normal operation, only for local debugging).
  
  **Live-verified**: rebuilt and restarted the stack; an unauthenticated `redis-cli ping` from outside the container now returns `NOAUTH Authentication required.` (previously `PONG`); the same command with `-a "${REDIS_PASSWORD}"` returns `PONG`; `docker port quizup_redis` now shows `127.0.0.1:6379` instead of `0.0.0.0:6379`/`[::]:6379`; both `backend` and `matchserver` health-checked healthy afterward, confirming they connect fine with the password.
  
  **Not yet done**: `docker-compose.prod.yml` and `docker-stack.yml` (the other two compose files) have the same unauthenticated-Redis pattern and were not fixed in this pass — flagging for a follow-up, since they weren't the file verified live and touching three compose files' worth of env-file chains in one pass risked more than this session budgeted for. Also not verified: whether the production EC2 host's security group already blocks external access to 6379 independent of this app-level fix (would need checking `deploy-ec2*.sh`/AWS console, out of scope for a source-only pass).
- **Severity: CRITICAL (as found). Live-verified before the fix, and live-verified fixed after.**
- File/line: `redis.conf:44` (`# requirepass your_redis_password_here` — commented out and, it turns out, irrelevant since the file isn't loaded), `docker-compose.yml` `redis` service (`ports: "${REDIS_PORT:-6379}:6379"`, no `bind 127.0.0.1` anywhere in `redis.conf`)
- Category: broken-access-control / infra-misconfiguration
- What happens: Redis has no password set, and the port is published to `0.0.0.0`/`[::]` — **live-verified** via `docker port quizup_redis` showing `6379/tcp -> 0.0.0.0:6379` and `[::]:6379`, plus a successful unauthenticated `PING` from outside the container. Anyone who can reach port 6379 — on the LAN today, and on the public internet in production unless a cloud security group happens to block it (a control this pass did not verify, since `deploy-ec2*.sh` is what actually provisions the EC2 host and wasn't audited for firewall rules) — gets full, unauthenticated read/write access to: every live match's state (`match:{matchId}` keys, readable/writable/forgeable), the auto-matchmaking queue, join-code mappings, the 60-second auth cache (`user:{id}:auth`, per Section 2's H1 finding — cached user objects, though not password hashes), and critically, the **Socket.io Redis adapter's pub/sub channel** that every matchserver worker subscribes to for cross-process event broadcast. Raw `PUBLISH` access to that channel is a lower-level, more powerful version of S1: an attacker doesn't even need to go through the app's socket handlers, they can inject fabricated events directly onto the bus that every connected client across every worker will receive as if the server sent them. A trivial `FLUSHALL` is also a one-command denial-of-service against every live match.
- Trigger: network reachability to port 6379. No credentials of any kind required.
- Maps to: not tied to a specific reported bug, but is the single highest-severity finding across all three passes of this audit — full compromise of live match state and the ability to forge server-originated events to every connected client, from an unauthenticated network position.
- Blast radius: total. This isn't a per-user or per-match issue; it's the entire Redis-backed state and event bus for the whole system.
- Fix sketch: (a) set `requirepass` in `redis.conf` (or `REDIS_PASSWORD` env var passed via `--requirepass`) and update every `REDIS_URL`/`REDIS_ADDR` consumer (`backend`, `matchserver`) to include it; (b) stop publishing the port to `0.0.0.0` in `docker-compose.yml` — Redis only needs to be reachable from `backend`/`matchserver` on the internal `quizup_network`, not from the host's public interface at all (drop the `ports:` mapping entirely, or bind it to `127.0.0.1:6379:6379` if host-side debugging access is needed). Did not implement in this pass — flagging because it also touches shared infra config (a category the operating rules ask me to confirm before changing) and because the exact production firewall posture (does the EC2 security group already block 6379?) needs to be checked before deciding how urgent the docker-compose change is versus a firewall-only fix.
- How to verify: after the fix, `redis-cli -h <host> ping` from outside the `quizup_network` (or from the host machine directly, as this pass did) should fail with `NOAUTH Authentication required.` or connection-refused, not `PONG`.

### [S4] `/api/friend-matches` was entirely unauthenticated; `POST /` trusted a client-supplied `userId`
- **Status: FIXED and live-verified (fifth pass — found while re-checking the user's own list).**
- File/line: `backend/src/server.ts:224` (`app.use("/api/friend-matches", friendMatchRoutes)` — no auth middleware), `backend/src/routes/friendMatchRoutes.ts:75-81` (`const userId = bodyUserId || req.user?.id || 1`)
- Category: broken-access-control / auth-bypass (the REST-side sibling of S1)
- What happens: the whole friend-match router was mounted with no auth middleware, so `req.user` was never populated and the create handler fell through to `bodyUserId || 1` — meaning **anyone, unauthenticated, could create a friend match as any `userId` they put in the request body** (or as user 1 by default). The `GET` routes were equally open: `GET /api/friend-matches` returns a list of *all* active matches (an unauthenticated monitoring endpoint), and `GET /code/:joinCode` / `GET /:matchId` leaked match metadata to anyone. With S1 now fixed on the socket side, this REST hole was the remaining unauthenticated way to spawn matches (resource/DB-row exhaustion, no rate limit) and to pollute match history under a spoofed creator identity.
- Trigger: a plain HTTP request, no credentials.
- Fix (applied): added `router.use(authenticateToken)` to `friendMatchRoutes` so every route requires a verified JWT, and rewrote `POST /` to take the creator identity from `req.user!.id`/`req.user!.username` and **ignore** any body-supplied `userId`/`username`. The frontend's `apiClient` interceptor already attaches the Bearer token to every `/friend-matches` call, so the real flow is unchanged.
- **Live-verified**: unauthenticated `POST /api/friend-matches` now returns `401` (was `200`); an authenticated create with a spoofed `userId: 99999999` in the body produced a match whose creator in Redis is the token's real user (`10004`), not the spoofed value — confirming the body value is fully ignored.

### [S5] `/api/matches` auth middleware was commented out — IDOR on pending-match reconnection state
- **Status: FIXED and live-verified (fifth pass).**
- File/line: `backend/src/routes/matchRoutes.ts:31` (`// router.use(authenticateToken);` — disabled "for testing"), `backend/src/controllers/matchController.ts` `checkPendingMatch` (was `req.params.userId`) and `clearPendingMatch` (was `req.params.userId`)
- Category: broken-access-control / IDOR
- What happens: `router.use(authenticateToken)` was commented out, leaving the entire `/api/matches` surface unauthenticated. The two pending-match endpoints took the target `userId` straight from the URL path with no ownership check: `GET /api/matches/pending/:userId` let anyone **read** any user's pending-match reconnection state (matchId, join code, question index — the join code being the sensitive part), and `DELETE /api/matches/pending/:userId` let anyone **delete** any user's pending match, silently denying that user's reconnection. The DELETE is the more damaging of the two — a pure griefing / denial-of-recovery primitive against a named victim, and it maps directly onto the project's first-class "a refresh always recovers a stuck match" requirement (an attacker can make that recovery fail on demand).
- Trigger: a plain HTTP request with a victim's numeric `userId` in the path; no credentials.
- Fix (applied): uncommented `router.use(authenticateToken)`, and changed both controllers to derive the user from `req.user.id` (the verified token) with a `403` if the path `:userId` doesn't match the caller's own id — so a user can only read or clear their own pending match. All frontend `/matches` calls go through the token-attaching `apiClient`, so the flow is unchanged.
- **Live-verified**: unauthenticated `GET` and `DELETE` on `/api/matches/pending/10001` now return `401`; an authenticated user reading their *own* pending match returns `200`, while reading another user's returns `403`.

### On the user's own 10-item list (verified this pass)

The user supplied a separate list of 10 intended fixes and asked whether they were done. **All 10 are already present in the working tree** (most landed pre-session; a few were completed earlier this session), verified by direct code read: (1) register TOCTOU → `create`+`UniqueConstraintError` catch, no `console.*` left; (2) login enumeration merged into `INVALID_CREDENTIALS` + dummy bcrypt compare + fire-and-forget `lastLoginAt`; (3) native `bcrypt` (no `bcryptjs` anywhere); (4) `express-rate-limit` `authLimiter` on `/register` + `/login` only; (5) `pingInterval:10000`/`pingTimeout:5000`/`transports:['websocket']` in `matchServerMaster.ts` (the `matchServer-enhanced.ts` half of that item is moot — that file was removed as dead code in Section 3); (6) `submitAnswer` hot path — `match.dbId` cache + `ensureDbMatchId` + fire-and-forget `.then().catch()` DB writes after the emits; (7) match-level stale reaper (`lastActivityAt` + 60s `setInterval` + `match_terminated` + `clearInterval` on shutdown); (8) every `match:`/`joincode:` write uses `setex` with a TTL (fixed 3600s rather than the item's "quiz-duration + 30 min / 2 h default" scheme — a valid safety net, since the stale reaper kills idle matches at 5 min; noted as a minor deviation, not a gap); (9) `middleware/auth.ts` Redis `user:{id}:auth` 60s cache; (10) `logger.ts` production `LOG_TO_CONSOLE` startup warning. Nothing on that list required a new fix — the new work this pass was S4/S5 above, which the list didn't mention.

### Checked, no finding (per the checklists loaded this pass)

- **Prototype pollution** — grepped for `__proto__`/`Object.assign(req...)`/deep-merge-of-user-input patterns across `backend/src`; none found. Redis-stored match JSON is parsed and read field-by-field (`match.workerId`, `match.status`, etc.), never spread (`...`) into a live object, so there's no reachable pollution sink even if a key like `__proto__` were injected into the stored JSON.
- **`child_process`/`eval`/OS command injection** — the only `eval(` in `backend/src` is `redisClient.eval(lua, ...)` in `matchService.ts:140`, which is Redis's own `EVAL` command running a fixed Lua script server-side (a standard Redlock-style distributed-lock pattern), not Node's `eval()` — no user input reaches it as code. No `child_process`/`execSync`/`exec(` calls exist in `backend/src` at all.
- **SQL injection** — all DB access goes through Sequelize's parameterized query builder (`.findOne`, `.create`, `.update` with `where:` objects); no raw `sequelize.query()` string-concatenation calls found in the audited files.
- **SSTI / template injection** — no server-side template engine (EJS/Pug/Handlebars) rendering user-controlled input was found; the app is a JSON API + separately-built SPA frontend.
- **Express `trust proxy` abuse** — not checked in this pass (not read in `server.ts`); flagging as a gap rather than a clean bill of health. Worth a follow-up grep for `app.set('trust proxy', ...)` combined with how `RATE_LIMIT`/IP-based logic in `rateLimiter.ts` derives the client IP, since a misconfigured trust-proxy setting would let `X-Forwarded-For` bypass the per-IP `authLimiter` audited as a fix in Section 2.
- **CSWSH in the classic cookie-auth sense** — doesn't apply the way the checklist expects, because the match server doesn't authenticate via an ambient cookie at all — it authenticates via a client-supplied payload with no verification (S1), which is a strictly worse variant of the same underlying problem (no Origin check needed, no victim browser needed, works from a plain script). Origin enforcement on the socket.io handshake was not separately checked, since S1 makes it moot: even a perfectly Origin-locked handshake wouldn't stop an attacker who can just claim any identity in the `authenticate` payload from any origin the server does accept connections from.
- **Race conditions beyond Section 1's match-state findings** — registration's TOCTOU was already fixed pre-session (Section 2, `UniqueConstraintError` catch). Did not find a monetary/credit/coupon system in this codebase to apply the classic double-redemption pattern against (QuizDash doesn't appear to have one). `submitAnswer`'s `player.hasSubmittedCurrent` check-then-set (`matchServerWorker.ts`, around line 905) is a candidate for a submit-twice race (two rapid `submit_answer` events for the same question from one connection landing before the first `hasSubmittedCurrent = true` write completes) but the whole worker is single-threaded per-match (no `await` sits between the check and the set within a single event-loop turn), so this specific check-then-act is not actually racy in Node's execution model — noted as checked, not a finding.

---

## SECTION 6 — Multi-quiz 5K+ concurrent-match stress test (sixth pass)

**[verified]** Ran on the live Docker Compose stack (4-core / 16 GB dev box), in-container generators on `quizup_quizup_network`, matches spread round-robin across all **52 quizzes** (IDs 102–153, 10 questions each) via the new `QUIZ_IDS` harness support (`backend/scripts/loadtest.js`, commit `df60d65`). User pool bulk-extended to **17,000 active users** (IDs 1–17,000; seeded 10,043–17,000 directly in Postgres, sequence bumped to 17000) to lift the old ~5,021-match seed cap.

### Capacity result (not the ceiling)

| Phase | Target | Result | matchserver CPU | matchserver mem | Event-loop lag | Conn errors |
|---|---|---|---|---|---|---|
| A | 500 / 52 quizzes | 500 (100%) | ~26% peak | 146 MiB | ~20 ms | 0 |
| B (3 gens) | 6,000 / 12,000 users | 6,000 (100%) | ~188% peak | 575 MiB | ~23 ms | 0 |
| C (4 gens) | 8,000 / 16,000 users | **7,970 / 15,940 (peak)** | ~193% peak | **833 MiB peak** | **~22 ms flat** | **0** |

- **Peak: 7,970 concurrent matches / 15,940 connected users, 0 connection errors** — comfortably past the >5,000 target and the prior single-quiz 5,000 run.
- **Worker balance stayed dead-even** at peak: `2073 / 2073 / 2072 / 2072` across the 4 autoscaled workers; per-worker `event_loop_lag_seconds` all ~0.022 s.
- **Multi-quiz cache burst is a non-event.** `pg_stat_database` before/after the first ramp: `blks_read` unchanged (`2602 → 2602`), `blks_hit +97,638` — the ~208 heavy nested question-loads (52 quizzes × 4 workers, each run once then cached 5 min per worker) were served **entirely from Postgres shared buffers, zero disk reads**. Postgres sat at 5–35% CPU during ramp; the only Postgres spike (100–134%) was the transient completion burst when 8,000 matches drained at once. Redis CPU stayed 8–20% but moved heavy traffic (2.12 GB in / 698 MB out at peak) from `saveMatchState` `setex` on every question advance — a scaling watch-item, not yet a bottleneck.
- **The ceiling is the box's 4 shared cores, not the match server.** Ramp time grew 129 s (3 generators, Phase B) to ~325 s (4 generators, Phase C) as generators + backend (friend-match create, up to 155% CPU) + Postgres + Redis all contended for the same 4 cores. The match server's event-loop lag stayed flat (~22 ms) the whole time — it was never the constraint. On dedicated cores with external load and separate DB/Redis hosts it would go materially higher.

### [M10] `matches` and `match_players` accumulate duplicate rows under concurrent play — non-atomic `findOne`-then-`create` with no unique constraint
- **Status: DOCUMENT-ONLY (found this pass, live-verified, not fixed).** This is the DB-persistence race the Section 5 "race conditions" note did not reach: that note correctly observed the single-threaded per-match event loop protects *in-memory* check-then-set (`hasSubmittedCurrent`), but the **fire-and-forget Postgres writes praised in item #6** each cross an `await` boundary and race one another with no DB-level backstop.
- File/line: `backend/src/matchServerWorker.ts` — `ensureDbMatch()` 1347–1375 (`Match.findOne({where:{matchId}})` then `Match.create`), `upsertDbPlayer()` 1385–1408 (`MatchPlayerModel.findOne({where:{matchId,userId}})` then `create`), both reached off the critical path from `submitAnswer` (1072–1087) and again from `endMatch` (1220–1255). No unique constraint exists on `matches.matchId` or `match_players (matchId,userId)` — verified: `matches` has only `matches_pkey(id)`, `match_players` only `match_players_pkey(id)`.
- Category: data-integrity / check-then-insert race (no unique-constraint backstop)
- What happens: on a match's first question, **both players submit near-simultaneously**. Each submission independently calls `ensureDbMatchId` then sees `match.dbId` still undefined then `ensureDbMatch` then `findOne({matchId})` returns null for both, so **both `INSERT` a `matches` row for the same `matchId`**. `endMatch` later updates only the one `id` cached in `match.dbId`, so the twin row is **left at `IN_PROGRESS` forever**. The same pattern in `upsertDbPlayer` (called once per answer, ×10, plus once in `endMatch`) produces duplicate `match_players` rows for the same `(matchId,userId)`, and orphan `PLAYING` rows on the twin `matches.id` that `endMatch`'s `FINISHED` update never sees. Answers key on the integer `matches.id`, so a match with two rows also has its first-question answers split across the twins.
- Trigger: two concurrent DB writes for one match before the first `create` commits — i.e. essentially every actively-played match. Invisible in `hold`-mode load (no answers, so only `endMatch` touches the DB, single path, no race), which is why the prior 5,000-match hold runs looked clean.
- Live evidence (clean isolated 300-match `play` batch, all matches id > watermark, otherwise-idle server):
  - **274 of 300 matches persisted and 274/274 COMPLETED with a `winnerId`** — gameplay/scoring is correct; the 26 that never persisted are a harness artifact (joiner socket join lost in the first fast ramp wave, never submitted an answer).
  - **106 of 274 matchIds (39%) got a duplicate `matches` row**; every duplicate sits at `IN_PROGRESS` while its twin is `COMPLETED`.
  - **`match_players`: 12 of 274 completed matches had 3 rows for 2 distinct users**; 121 leftover `PLAYING` rows never flipped to `FINISHED`.
  - **Answer invariant**: 168 match-rows had the exact 20 (`2×10`); 103 had 19 (one answer landed on the twin row or was dropped by the generator's timer); tiny 17–18 tail.
  - Pre-existing pollution: the `matches` table already holds **2,000+ stale `IN_PROGRESS` rows** dating back across prior sessions (07-13 onward) — the historical accumulation of this same race plus [M11].
- Severity: medium — does **not** corrupt live gameplay or the winner/score result (in-memory state is the source of truth and is correct), but it corrupts the Postgres audit/history tables: any status-based query, match-count, or per-user history analytic over `matches`/`match_players` is inflated and includes permanently-stuck `IN_PROGRESS`/`PLAYING` ghost rows.
- Fix sketch: add a unique constraint on `matches.matchId` and on `match_players (matchId,userId)`, then convert both writes to a real atomic upsert (`INSERT … ON CONFLICT DO UPDATE`, i.e. Sequelize `upsert`/`findOrCreate` guarded by the constraint) so a losing racer updates instead of inserting a twin. A cheaper interim mitigation: gate `ensureDbMatch` behind a per-match in-memory promise (`match.dbIdPromise`) so the second concurrent caller awaits the first `create` instead of issuing its own `findOne`.
- How to verify: re-run a `play` batch and assert `count(*) = count(distinct matchId)` on the new `matches` rows and `count(match_players) = 2×matches` — currently 380 rows for 274 matchIds; after the fix it should be exactly 274 and 548.

### [M11] `terminateStaleMatch` never writes a terminal status to Postgres
- **Status: DOCUMENT-ONLY (found this pass, verified by code read + DB state).**
- File/line: `backend/src/matchServerWorker.ts` `terminateStaleMatch()` 126–153 — clears the in-memory match, deletes the Redis snapshot, and `notifyMaster({type:'match_completed'})` so master pool bookkeeping and the live `active_matches` metric stay correct, but issues **no `Match.update({status})`**. (`reapStaleMatches` 114–124 is the caller.)
- Category: data-integrity / missing DB reconciliation
- What happens: a match abandoned mid-play (both sockets gone) is reaped from memory and Redis after the 5-min idle timeout, and the live view is correct — but its Postgres `matches` row keeps whatever non-terminal status it last had (`IN_PROGRESS`, or `WAITING` if it never started). It is never reconciled to `COMPLETED`/an `ABANDONED` state. This is the second contributor (alongside [M10]'s twin rows) to the 2,000+ stale `IN_PROGRESS` rows in the table.
- Severity: low–medium — observability/analytics only; the live metrics are honest (they're driven off the master's in-memory count, not the DB).
- Fix sketch: in `terminateStaleMatch`, before dropping the match, `ensureDbMatch(match).then(m => m?.update({status:'ABANDONED', endedAt:new Date()}))` (add an `ABANDONED` enum value, or reuse `COMPLETED` with a null `winnerId`), mirroring `endMatch`'s persistence tail.
- How to verify: abandon a match (open a friend match, submit one answer, drop both sockets), wait past `STALE_MATCH_TIMEOUT_MS`, and confirm the `matches` row moves off `IN_PROGRESS`.

### [M12] Observability nits surfaced at scale (graceful worker exits logged as errors; stale per-worker gauge)
- **Status: DOCUMENT-ONLY (found this pass, verified in `logs/matchserver/error.log` + live `/metrics`).** In scope because the task asked to confirm the system stays *observable* at scale — it does, with these two caveats.
- **(a) Graceful worker shutdowns are logged at `level:error` as "Worker died" with a ChildProcess stack trace.** File: `backend/src/services/enhancedWorkerPool.ts:39` (the `worker.on('exit')` handler). Every "Worker died" entry across the entire (bind-mounted, multi-run) log history is **exit code `(0)`** — `11× Worker 1`, `9× Worker 2`, `8× Worker 3`, `2× Worker 4`, **zero non-zero exits**. No worker ever crashed, including under the 8,000-match peak; the container's `RestartCount` is 0 and `OOMKilled` is false. So these are the autoscaler cycling idle workers, recorded as errors. Effect: `error.log` is dominated by benign events (28 of ~37 recent error entries are "Worker died"), which would page on-call for nothing and buries the real signal. Fix sketch: branch on `code`/`signal` in the exit handler — `code===0` (or an intentional-shutdown flag set by the pool's scale-down path) → `logInfo`; only unexpected exits → `logError`.
- **(b) `matchserver_worker_matches` per-worker gauge goes stale.** After all load drained, `/metrics` reported `active_matches_total 0` and `connected_users 0` (both authoritative and correct) while `matchserver_worker_matches{worker_id=1..3}` still summed to 25 phantom matches, stable across repeated scrapes. The per-worker gauge isn't zeroed on worker cycle / certain match-exit paths. Effect: a per-worker "matches per worker" dashboard panel can show phantom load on an idle server; the aggregate is fine. Fix sketch: reset the worker's gauge to its live `this.matches.size` on each heartbeat rather than only incrementing/decrementing on events, and clear it when a worker is removed from the pool.
- **(c) Rare join race under heavy ramp (not new, quantified here): "Player still not in match after retries".** File: `backend/src/matchServerWorker.ts` `playerReady` ~590, via `clientReady` ~642. 9 occurrences in the current `error.log` across *multiple* sessions (07-19 → 07-21) — a player's `CLIENT_READY` reaches the assigned worker before the join propagated master→worker, and the worker gives up after retries. Low rate, but it is the mechanism behind the handful of matches that never start under a fast ramp wave (e.g. 26 of 300 in the clean play batch). Not a capacity blocker; worth a longer/backing-off retry or a deferred-ready queue if the never-started fraction matters.

### Fix status — [M10]/[M11]/[M12a] applied and verified (this session)

**Code:** `backend/src/matchServerWorker.ts` (`ensureDbMatch`→`findOrCreate`, per-match `dbIdPromise` guard in `ensureDbMatchId`, `upsertDbPlayer`→`findOrCreate`, `endMatch` player loop→`findOrCreate`, `terminateStaleMatch` now writes `CANCELLED`+`endedAt`), `backend/src/services/enhancedWorkerPool.ts` (cluster `exit` handler: clean/intentional exits log at info, only unexpected deaths at error; `intentionalStops` set), `backend/src/models/{Match,MatchPlayer}.ts` (unique indexes), `backend/src/migrations/add-match-unique-constraints.sql` (FK-safe dedupe + `matches_matchid_uq` + `match_players_matchid_userid_uq`), `docker-compose.yml` (stale-reaper tunables plumbed).

**Migration result (existing DB):** de-duplicated to **18,006 matches rows = 18,006 distinct matchIds** (was 2,090 duplicate matchId groups + 841 duplicate player groups → **0** of each); both unique indexes created; backend alter-sync accepts them.

**[M10] verified** — a 300-match `play` batch on the fixed build: **296 rows / 296 distinct matchIds (0 duplicate matches)**, every completed match has **exactly 2 FINISHED player rows for 2 distinct users (0 duplicate players, 0 orphan PLAYING)**, and **every match has exactly 20 `match_answers` (2×10)** — 296/296 on the invariant, vs 168/274 before. 296/296 completed with a winner.

**[M11] verified** — with the stale timeout dropped below the question timeout and the generator killed mid-play, **37 abandoned matches were reaped to `CANCELLED` with `endedAt` set (0 left IN_PROGRESS)**; before, those stayed IN_PROGRESS forever.

**[M12a] applied** — routine worker scale-down/shutdown now logs `Worker exited` at info; only unexpected exits log `Worker died unexpectedly` at error. ([M12b] stale per-worker gauge and [M12c] join-race retry remain open — not addressed this pass.)

---

## SECTION 7 — AUTO matchmaking overhaul (Redis-backed queue, ELO, multi-replica)

**[verified]** Replaced the previous "hot-patched" AUTO matchmaking with a Redis-backed shared queue (`backend/src/services/matchmakingQueue.ts`, per-category sorted sets + a distributed-lock `claimPair`), a central sweep loop (widens ELO range, pairs longest-waiter→closest-ELO, times out, emits `matchmaking_update` with queue position/ETA), ranked ELO on AUTO matches only (`backend/src/utils/elo.ts`, standard Elo K=32), a consolidated frontend hook (`Frontend-admin/src/hooks/useAutoMatchmaking.ts`), and multi-replica infra (`docker-compose.yml`/`docker-compose.prod.yml`/`nginx.conf` — removed the fixed `matchserver` container name so `--scale` works, `SERVER_ID` from container hostname). Design + socket contract: [docs/MATCHMAKING.md](docs/MATCHMAKING.md). Human-vs-human only — no AI-opponent fallback. Three real bugs were found and fixed during verification, all live-tested against the running stack, not just reasoned:

### [M13] `chooseQuizId` broken `GROUP BY` — blocked every AUTO pairing attempt
- **Status: FIXED and verified.**
- File/line: `backend/src/matchServerMaster.ts`, `chooseQuizId` (the `QuizQuestion.findAll` call feeding the AUTO-match quiz pool).
- Category: correctness / pre-existing bug (predates this session's workstreams — same query shape as the original code; the overhaul only added caching around it).
- What happens: `group: ['QuizQuestion.quizId']` produces invalid SQL (`missing FROM-clause entry for table "QuizQuestion"` — Postgres wants the bare output-list alias, not a dotted model-class-qualified name, in `GROUP BY` for this query shape). Every single AUTO pairing attempt failed with "Could not start the match right now."
- Trigger: any AUTO matchmaking request. This is very likely why AUTO matchmaking felt broken historically — it may never have worked end-to-end.
- Fix: `group: ['quizId']` (bare attribute name). Verified against a standalone Sequelize repro script + the compiled query via `psql` before touching source.
- How verified: `MODE=auto` load test, 100 searchers/category — 0/200 matched (all errored) before the fix, 200/200 matched after.

### [M14] `joinMatch` Redis-hydration race — AUTO matches hang at WAITING forever
- **Status: FIXED and verified.**
- File/line: `backend/src/matchServerWorker.ts`, `joinMatch()` / new `hydrateMatchFromRedis()`.
- Category: race condition (async hydration, no lock) — same family as [M6]/[M9] (concurrent access to worker-local match state) but in the join path, not teardown.
- What happens: both AUTO players' `join_match` IPC land back-to-back with **neither** having created the match room locally first (unlike FRIEND matches, where the creator's synchronous `create_friend_match` already puts the match in `this.matches` before the joiner's `join_match_by_code` ever reaches a worker). Since `joinMatch` is `async` with an `await this.redis.get(...)` before `this.matches.set(matchId, match)`, two concurrent calls for the same **new** matchId both pass `if (!match)`, each build a **separate** `MatchRoom` object with its own player-restore, and the second `.set()` silently clobbers the first — losing that player's `socketId`. `connectedCount` can never reach `maxPlayers`, so `LOAD_GAME_SCENE` never fires and the match hangs forever. Confirmed via structured logs: `match_created` fired twice for one matchId (`workerMatchCount: 1` then `2`) — the tell.
- Fix: `hydratingMatches: Map<string, Promise<MatchRoom>>` — concurrent callers for the same matchId now share one in-flight hydration. Also fixed a related gap this exposed: the master's `userToMatch` map (load-bearing for cross-replica routing via `getUserMatch`) was only populated from `match_created`/`player_joined` IPC; since `match_created` now fires once per match instead of once per caller, the reconnect branch (which both AUTO players take) needed its own `notifyMaster({type:'player_joined', ...})` so every joiner is tracked regardless of which branch it hits.
- How verified: `MODE=auto` load test, 300 searchers — 300/300 matched, 150 unique matchIds × 2 exactly, 0 errors (regression after the fix); full AUTO matches (tie and forced win/loss) completed end-to-end with correct ELO deltas.

### [M15] Cross-replica `serverId` silently dropped on every worker-side Redis write — AUTO matches stall until the 30s question timeout when players land on different replicas
- **Status: FIXED and verified.**
- File/line: `backend/src/matchServerWorker.ts` — the `MatchRoom` interface (missing a `serverId` field entirely), all three ad-hoc Redis-hydration sites that build a `MatchRoom` from a parsed Redis blob (`hydrateMatchFromRedis`, `playerReady`'s inline hydrate, `submitAnswer`'s inline hydrate), and `saveMatchState` (the object it serializes back to Redis).
- Category: data-loss / silent field-stripping on read-modify-write.
- What happens: `finalizePair` (`matchServerMaster.ts`) stamps the popping replica's `SERVER_ID` onto the match's Redis blob (`match:<id>.serverId`) so `routeMatchEvent`/the `mm_match_event` handler can tell which replica owns a match and forward client events there instead of processing them locally. But the **worker-side** `MatchRoom` TypeScript interface never had a `serverId` field, so every Redis hydration (join/playerReady/submitAnswer) built a `MatchRoom` that silently dropped it, and every subsequent `saveMatchState()` call (join, ready, `startMatch`, every question advance) overwrote the Redis blob with a fresh object that had never carried it — permanently erasing `serverId` after the *first* worker-side write. Once gone, `match.serverId && match.serverId !== SERVER_ID` evaluates `false` (falsy short-circuit) on **every** replica, so every replica believes it's the owner and processes `submit_answer` "locally" — including replicas that never even joined the match, which then independently ad-hoc-hydrate their own divergent copy of the `MatchRoom` (via the same missing-dedup inline-hydrate branches noted in [M14]'s fix). Each replica's copy only ever sees its own local player's submission, so `allSubmitted` never turns true on either copy, and the question only ever advances via the 30s+5s server-side timeout fallback (see [M3]) — not the intended immediate advance.
- Trigger: only reachable with `docker compose up -d --scale matchserver=2+` (opt-in; the documented default is 1 replica, where `match.serverId === SERVER_ID` is trivially always true and the whole class of bug is unreachable). Reproduced by pinning two AUTO players to different replica containers directly (`MATCH_URL=http://quizup-matchserver-1:3001` / `-2:3001`).
- Live evidence: with debug logging temporarily added to `routeMatchEvent`/`submitAnswer`, the routing decision for every `submit_answer` event showed `decision:"local"` on **both** replicas with `matchServerId` **absent from the log line** (i.e. `undefined` — `JSON.stringify` drops it), confirming the field was gone from Redis by the time any question was answered, not merely misread.
- Fix: added `serverId?: string` to the `MatchRoom` interface, populated it from `storedMatch.serverId` at all three hydration sites, and included `serverId: match.serverId` in `saveMatchState`'s persisted object. Unset for FRIEND matches, unaffected (never had a `serverId` to begin with — see the routing comment in `matchServerMaster.ts`).
- How verified: two players pinned to different replica containers, category 283 — before the fix, each question advanced only via the 30–35s timeout (a 10-question match took 60s+ and never completed inside the harness's window); after the fix, `submit_answer` routing showed a stable `matchServerId` across all 10 questions (`decision:"local"` only on the true owner, `decision:"forward"` on the peer), and the match completed in **~10s** across three separate runs (forced win/loss and a tie, with replica assignment swapped between runs) — win/loss produced exactly `1200→1216`/`1200→1184` (K=32 Elo math), tie produced no rating change. Regression: single-replica `MODE=auto` load test (150 intended matches / 300 searchers, category 283) after the fix — 298/300 matched, 0 errors, 0 double-matches, invariant holds.

**Verification summary (this session):** single-replica AUTO path fully verified correct (pairing, join, ELO tie/win/loss, AUTO-only ELO gate excluding FRIEND matches). Multi-replica AUTO path — previously **not production-ready** due to [M15] — is now fixed and verified correct across three independent 2-replica runs. `docker compose up -d` still defaults to 1 replica; scaling to 2+ is opt-in via `--scale matchserver=N` (remember `docker compose restart nginx` after any scale change — it caches upstream IPs, see the Gotchas in `CLAUDE.md`).
